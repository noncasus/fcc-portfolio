( function( $ ) {
 
    $( function() { // Ready
 
        var settings = { 
            anchors: 'a'
        };
 
        $( '.container' ).smoothState( settings );
    } );
 
})( jQuery );